const {
  remoteBuild,
  buildAllExtensions,
  buildSingleExtension,
} = require('./lib/build');

const DevModeInterface = require('./lib/DevModeInterface');

module.exports = {
  remoteBuild,
  buildAllExtensions,
  buildSingleExtension,
  DevModeInterface,
};
